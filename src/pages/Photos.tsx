import { PagePlaceholder } from '../components/PagePlaceholder'

export function Photos() {
  // TODO(photos): list photos for the project from the `photos` table, resolve
  // storage_path via supabase.storage, and add a mobile-first upload flow
  // (camera capture) for foreman/worker. Grid must work on a phone screen.
  return (
    <PagePlaceholder
      title="Фото"
      description="Фотоотчёт по объекту и этапам — загрузка с телефона прямо на площадке"
    />
  )
}
